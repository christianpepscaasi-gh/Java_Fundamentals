public class math_class {
    public static void main(String[] args) {
        // System.out.println(Math.PI);//print math class pi
        // System.out.println(Math.E);//Exponential Constant, Eul's Number

        double result;

        result = Math.pow(2, 3);// 2 to the power of 3
        // result = Math.abs(-5); //will create an absolute number, meaning all negative
        // number will turned into postive
        // result = Math.sqr(9); //square root
        // result = Math.round(3.14); round to the nearest number
        // result = Math.ceil(3.14); round up
        // result = Math.floor(3.14); round down
        // result = Math.max(10, 20); take the max number
        // result = Math.min(10,20); take the minimum number

        System.out.println(result);
    }
}
